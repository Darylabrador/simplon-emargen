<% if (total != 0) { %>
    <div class="d-flex justify-content-center my-1 pb-5">
        <nav>
            <ul class="pagination">

                <% if (currentPage != 1  && previousPage != 1) { %>
                <li class="page-item mx-2">
                    <a class="page-link" href="?page=1"> 1 </a>
                </li>
                <% } %>
                
                <% if(hasPreviousPage) { %>
                <li class="page-item mx-2">
                    <a class="page-link" href="?page=<%= previousPage %>"> <%= previousPage %> </a>
                </li>
                <% } %>
                
                <li class="page-item active mx-2">
                    <a class='page-link' href="?page=<%= currentPage %>"> <%= currentPage %> </a>
                </li>
                
                <% if(hasNextPage) { %>
                <li class="page-item mx-2">
                    <a class="page-link" href="?page=<%= nextPage %>"> <%= nextPage %> </a>
                </li>
                <% } %>
                
                <% if(lastPage != currentPage && nextPage != lastPage) { %>
                <li class="page-item mx-2">
                    <a class="page-link" href="?page=<%= lastPage %>"> <%= lastPage %> </a>
                </li>
                <% } %>
            </ul>
        </nav>
    </div>
<% } %>